<h1>Cookbook</h1>
<h2>Categories:</h2>
<ul>
  <% @categories.each do |category| %>
    <li>
      <%= link_to category.name, category_path(category) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a Category", new_category_path, class: "add-category-link" %>
